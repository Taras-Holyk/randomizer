version: '3.7'
services:
  backend:
    build: ./backend
    command: sails lift
    ports:
      - "1337:1337"
    volumes:
      - ./backend:/srv/backend
    restart: on-failure
    links:
      - db
  db:
    image: "postgres"
    container_name: "postgres"
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    restart: on-failure
